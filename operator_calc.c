#include <stdio.h>

int main() {
     int a, b,c,d;

     a = 5;
     b = 2;
     c = 3;
     d = 15;

    int sum = a+b;
    printf("Sum of a & b is :  %d\n", sum);

    int subs = a-b;
    printf("Substraction of a & b is : %d\n", subs);

    int multiply = a*b;
    printf("Multiply of a & b is : %d\n", multiply);

     int division = a/d;
     printf("Division of a & d is : %d\n", division);

     return 0;
}
